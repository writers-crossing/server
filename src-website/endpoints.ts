import { Request, Response } from 'express';
import { User, Sprint, WcEntry } from './app/entities'

export const getUser = (req: Request, res: Response) => {
    // Endpoint logic
}

export const getUserAvatarPng = (req: Request, res: Response) => {
    // Endpoint Logic
}

export const createUser = (req: Request, res: Response) => {
    // Endpoint logic
}